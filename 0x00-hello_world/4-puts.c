#include <stdio.h>
#include <string.h>

/**
 * main - print a string
 * Return: 0 if no error
 */
int main(void)
{
	char str1[] = "\"Programming is like building a multilingual puzzle";

	puts(str1);
	return (0);
}
