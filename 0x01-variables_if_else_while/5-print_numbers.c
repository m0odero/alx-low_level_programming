#include <stdio.h>

/**
 * main - print one digit numbers followed by a newline
 * Return: 0 if successful
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
